`const express = require('express');
const { {{name}}Controller } = require('../controllers');
const api = express.Router();

//api.post('/v1/{{name}}/test', {{name}}Controller.{{name}}Test());
module.exports = api;`
