`const express = require('express');
const { {{cname}}Controller } = require('../controllers');
const api = express.Router();

//api.get('/v1/{{name}}/test', {{cname}}Controller.{{name}}Test);
module.exports = api;`
