`const express = require('express');
const { {{cname}}Controller } = require('../controllers');
const api = express.Router();

//api.post('/v1/{{name}}/test', {{cname}}Controller.{{name}}Test());
module.exports = api;`
